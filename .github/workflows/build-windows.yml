name: Build Windows

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Haxe
        uses: krdlab/setup-haxe@v1
        with:
          haxe-version: 4.1.5
          neko-version: 2.3.0

      - name: Set up haxelib repository
        shell: pwsh
        run: |
          $haxelibPath = Join-Path $env:RUNNER_TEMP "haxelib"
          New-Item -ItemType Directory -Force -Path $haxelibPath | Out-Null
          $haxelibPath | haxelib setup
          "HAXELIB_PATH=$haxelibPath" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install Haxe dependencies
        shell: pwsh
        run: |
          haxelib install lime 7.9.0 --quiet
          haxelib install openfl --quiet
          haxelib install flixel --quiet
          haxelib run lime setup flixel
          haxelib run lime setup
          haxelib install flixel-tools --quiet
          haxelib install flixel-addons --quiet
          haxelib install flixel-ui --quiet
          haxelib install hscript --quiet
          haxelib install actuate --quiet
          haxelib install newgrounds --quiet
          haxelib git polymod https://github.com/larsiusprime/polymod.git
          haxelib git discord_rpc https://github.com/Aidan63/linc_discord-rpc

      - name: Install extension-webm
        shell: pwsh
        run: |
          haxelib install extension-webm --quiet
          haxelib run lime rebuild extension-webm windows -64

      - name: Show installed libraries
        shell: pwsh
        run: haxelib list

      - name: Build Windows release
        shell: pwsh
        run: haxelib run lime build windows

      - name: Upload Windows build
        uses: actions/upload-artifact@v4
        with:
          name: BOB-windows
          path: exportJon/release/windows/bin
          if-no-files-found: error
